# ============================================================
# 0101 - Docker Compose for Full Stack
# ============================================================
# Usage:
#   docker compose up -d          # Start all services
#   docker compose down           # Stop all services
#   docker compose logs -f        # View logs
#   docker compose up --build     # Rebuild and start
# ============================================================

version: "3.8"

services:
  # ----------------------------------------------------------
  # Backend: FastAPI + Uvicorn
  # ----------------------------------------------------------
  0101-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: 0101-backend
    ports:
      - "8088:8088"
    env_file:
      - .env
    environment:
      - CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://0101-ui:3000
      - WORKSPACE_DIR=/app/data/workspace
    volumes:
      # Persist user data across container restarts
      - 0101-data:/app/data
      # Mount code for development (comment out for production)
      # - ./core:/app/core:ro
      # - ./execution:/app/execution:ro
      # - ./skills:/app/skills:ro
      # - ./directives:/app/directives:ro
    depends_on:
      - 0101-healthcheck
    restart: unless-stopped
    networks:
      - 0101-network
    # Resource limits
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2G
        reservations:
          cpus: "0.5"
          memory: 512M

  # ----------------------------------------------------------
  # Frontend: React + Tauri (UI)
  # ----------------------------------------------------------
  0101-ui:
    build:
      context: ./ui
      dockerfile: Dockerfile.ui
    container_name: 0101-ui
    ports:
      - "3000:3000"
    environment:
      - VITE_API_URL=http://0101-backend:8088
      - VITE_WS_URL=ws://0101-backend:8088
    depends_on:
      - 0101-backend
    restart: unless-stopped
    networks:
      - 0101-network
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M

  # ----------------------------------------------------------
  # Healthcheck: Wait for backend to be ready
  # ----------------------------------------------------------
  0101-healthcheck:
    image: alpine:3.18
    container_name: 0101-healthcheck
    command: >
      sh -c "
        echo 'Waiting for backend to be ready...' &&
        for i in $(seq 1 30); do
          if wget -qO- http://0101-backend:8088/health 2>/dev/null; then
            echo 'Backend is ready!' &&
            exit 0
          fi
          echo 'Waiting... ($i/30)'
          sleep 2
        done &&
        echo 'Backend not ready after 60 seconds' &&
        exit 1
      "
    networks:
      - 0101-network
    profiles:
      - healthcheck

# ----------------------------------------------------------
# Named volumes for persistent data
# ----------------------------------------------------------
volumes:
  0101-data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ./data

# ----------------------------------------------------------
# Networks
# ----------------------------------------------------------
networks:
  0101-network:
    driver: bridge
    name: 0101-network
